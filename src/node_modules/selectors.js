import R from 'ramda'

const getFighterById = (state, id) => R.prop(id, state.fighters)

export const getFighters = state => {
  const fighters = R.map(id => getFighterById(state, id), state.fightersPage.ids)
  return fighters
}
