import { callApi } from 'api/apiHelper'

export const fetchFighters = async () => {
  try {
    const endpoint = '5d875bb134000063480a14c7';
    const apiResult = await callApi(endpoint, 'GET');
    console.log(apiResult)
    return apiResult.content;
  } catch (error) {
    throw error;
  }
}